jwt:
  expiration-time: 604800 # JWT token expiration time in seconds (one week)
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/sql_db
    username: root
    password: password
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    database: mysql
    show-sql: true
