spring:
  datasource:
    url: jdbc:mysql://localhost:3306/sql_db
    username: root
    password: password
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: create
    database: mysql
    show-sql: true
